# Bubble-Shooter-Senior 開發計畫

本計畫採小步驟推進。每一階段完成後，先由 Codex 做技術檢查，再由使用者在桌面瀏覽器實測並確認，才進入下一階段。

## 0. 專案基礎（本次）

- 檢查目前資料夾與 Git 狀態。
- 建立 `AGENTS.md`，固定產品目標、參考參數、著作權界線與分工。
- 建立本開發計畫。

## 1. 最小可執行前端骨架

- 建立最精簡的 Vite + 原生 JavaScript/HTML/CSS 專案。
- 建立基本遊戲頁面、Canvas 容器與清楚的啟動指令。
- 先確認桌面瀏覽器可以載入、調整視窗尺寸並正常重繪；不加入完整遊戲邏輯。

## 2. 遊戲座標與靜態棋盤

- 集中建立固定參考參數。
- 實作 grid / cell 座標轉換與六色泡泡的清楚視覺樣式。
- 顯示可辨識的發射器與測試棋盤，先由使用者確認尺寸、對比與版面。

## 3. 瞄準、射擊與反彈

- 實作滑鼠瞄準角度。
- 依 `bubbleSpeed: 12` 實作泡泡飛行。
- 實作左右牆反彈與可重現的碰撞檢查，並以 `collisionDiameter: 25` 與 `bubbleDiameter: 50` 為基準。
- 由使用者實測射擊速度、軌跡與反彈手感。

## 4. 配對、掉落與回合流程

- 實作同色連接判定、消除門檻、懸空泡泡掉落與下一顆泡泡流程。
- 先建立單純且可測試的遊戲狀態，不加入額外模式或成就系統。
- 補上核心邏輯測試與邊界案例。

## 5. 可玩原型與體驗調整

- 補上分數、剩餘泡泡／失誤等必要資訊與基本重新開始流程。
- 調整字級、對比、按鈕與提示，使操作清楚但不改變核心難度。
- 使用者在桌面瀏覽器完成一輪實際遊玩與回饋。

## 6. 穩定化與後續適配

- 補齊瀏覽器相容性、錯誤處理、效能與可維護性檢查。
- 在桌面版本確認後，再規劃手機／平板輸入與版面適配。
- 是否加入任何非核心功能，須另行提出並確認；預設維持無廣告、單一遊戲頁面。

## 下一步建議

先進入第 1 階段：建立可啟動的最小前端骨架，並只驗證頁面載入、Canvas 顯示與專案指令。完成後再開始座標與靜態棋盤，不要在同一個變更中提前加入射擊或完整遊戲規則。
