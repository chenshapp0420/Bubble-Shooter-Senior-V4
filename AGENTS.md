# Bubble-Shooter-Senior 開發規範

## 專案定位

Bubble-Shooter-Senior 是一款以桌面瀏覽器為第一階段目標的網頁版 Bubble Shooter，主要服務熟悉傳統 Bubble Shooter 的長者使用者。遊戲應該清楚、單純、容易操作，但不因為目標使用者是長者而刻意降低遊戲難度。

## 產品目標與範圍

- 以滑鼠瞄準與射擊為主要操作方式。
- 泡泡必須夠大、輪廓清楚、顏色容易辨識。
- 遊戲節奏、射擊速度、碰撞與反彈手感，以目前研究中的 MSN Bubble Shooter 行為作為參考基準。
- 第一階段只優先支援桌面瀏覽器；手機與平板適配列入後續階段。
- 不加入廣告。
- 不加入其他遊戲或與 Bubble Shooter 無關的功能。
- 遊戲頁面保持乾淨、單純，以遊戲本身為核心。

## 固定參考規格

以下數值是目前從 MSN Bubble Shooter JavaScript 觀察到的參考基準，除非產品負責人明確要求調整，不得自行改寫：

```text
bubbleSpeed: 12
collisionDiameter: 25
bubbleDiameter: 50
horizontalMargin: 40
totalBubbleColors: 6
```

目前已確認的行為與概念包括：射擊角度計算、泡泡飛行軌跡、左右牆反彈、`collisionX` / `collisionY`、`collRadius`、`bubbleSpeed`、grid / cell 座標，以及 `BubbleShot` 類型的遊戲事件。這些是行為與設計參考，不是複製原始程式碼的授權。

## 技術與架構原則

- 目前專案是空白 Git 工作資料夾；首次實作時採用最精簡的瀏覽器前端架構。
- 建議初始方案：Vite + 原生 JavaScript/HTML/CSS，以 Canvas 作為遊戲渲染層；只有在介面狀態確實需要時才增加 UI 元件層。
- 遊戲核心應與畫面繪製、輸入處理分離，至少區分遊戲狀態、泡泡／網格模型、碰撞與反彈、輸入與渲染。
- 優先建立可測試的純函式與小型模組；不要在第一階段引入不必要的後端、資料庫、登入、廣告或分析服務。
- 固定參考參數應集中管理，不散落在各個模組中。
- 所有時間、尺寸與座標的單位要在命名或註解中清楚表達；避免用難以追蹤的魔法數字。
- 先支援現代桌面瀏覽器，並在每個階段保留清楚的手動測試方式。

## 開發與分工

- Codex：檢查專案、建立與修改程式碼和文件、執行可重現的檢查、回報實際結果。
- ChatGPT：負責需求規劃、審查、優先級與後續工作指令。
- 使用者：負責在實際瀏覽器中測試操作、手感、可讀性與整體體驗，並確認是否符合預期。

每次變更都必須清楚說明新增檔案、修改檔案、實際完成內容、驗證結果與下一步建議。除非使用者明確要求，不得一次完成整款遊戲，也不得自行擴大功能範圍。

## 參考資料與著作權界線

- 只根據已觀察到的遊戲行為、公開玩法概念與本文件記錄的參考參數重新實作。
- 不複製、貼上、反編譯、改名或移植 MSN Bubble Shooter 的受版權保護原始碼、資產或內部實作。
- 若未來需要新增參考資料，應記錄其來源、用途與限制；不得把參考資料誤稱為本專案原始碼。

## 變更控制

- 小步提交工作成果；每次只處理一個清楚的工作目標。
- 不在未授權下刪除既有檔案、加入外部服務、提交 Git、推送遠端或部署。
- 任何參數調整、操作規則變更或新功能，都要先在工作說明中提出理由，並由產品負責人確認。
- 保持文件與實際程式一致；若架構選擇改變，先更新本文件與開發計畫。
